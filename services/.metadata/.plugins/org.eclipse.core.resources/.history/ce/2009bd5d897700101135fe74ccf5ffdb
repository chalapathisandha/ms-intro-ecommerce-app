package com.chalapathi;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.chalapathi.ecommerce.customer.CustomerClient;
import com.chalapathi.ecommerce.kafka.OrderProducer;
import com.chalapathi.ecommerce.order.OrderMapper;
import com.chalapathi.ecommerce.order.OrderRepository;
import com.chalapathi.ecommerce.orderline.OrderLineService;
import com.chalapathi.ecommerce.payment.PaymentClient;
import com.chalapathi.ecommerce.product.ProductClient;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.RequiredArgsConstructor;
import lombok.Setter;
import lombok.RequiredArgsConstructor;

@RestController
@RequestMapping("/load-test/api/v1")
@RequiredArgsConstructor
public class HelloController {

	private final ProductClient productClient;

	/*
	 * @GetMapping("/load-products") public ResponseEntity<ProductResp>
	 * loadProductsFromProductService() { productClient = return
	 * ResponseEntity.ok(productClient.loadProducts()); }
	 */
}



@AllArgsConstructor
@NoArgsConstructor
@Getter
@Setter
@Builder
class ProductResp {
	
	private String environment;
	private List<String>  products;

}
